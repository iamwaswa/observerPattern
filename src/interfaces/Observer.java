package interfaces;

/**
 * The interfaces.Observer interface describes
 * an implementable interface
 * for an observer of the model.NumberGenerator.
 *
 * @author Waswa Olunga
 */

public interface Observer {

	void stateChanged(int num);
}